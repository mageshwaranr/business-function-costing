var CostPricing = CostPricing || {};

CostPricing.module = (function () {
    var fn, api;
    
    fn = {
        _init: function () {
            
        }
    };
    
    api = {
        init: function () {
            return fn._init.apply(this, arguments);
        }
    };
    
    return api;
})();